[Pixel][UnitTest]
struct VariableIf
{
  [EntryPoint]
  function Main()
  {
    var condition = true;
    var value = 1;
    // Test an if on a variable (requires an OpLoad)
    if(condition)
    {
      value = 1;
    }
    else if(!condition)
    {
      value = 2;
    }
  }
}
