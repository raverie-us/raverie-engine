///////////////////////////////////////////////////////////////////////////////
///
/// \file MathTypedefs.hpp
/// The typedefs of the main types in the math library. Separate
/// from the math standard to avoid some weird compiler issues
/// about including multiple times.
///
/// Authors: Joshua Davis
/// Copyright 2010-2012, DigiPen Institute of Technology
///
///////////////////////////////////////////////////////////////////////////////
using Math::real;

using Math::BoolVec2;
using Math::BoolVec2Param;
using Math::BoolVec2Ref;
using Math::BoolVec2Ptr;

using Math::BoolVec3;
using Math::BoolVec3Param;
using Math::BoolVec3Ref;
using Math::BoolVec3Ptr;

using Math::BoolVec4;
using Math::BoolVec4Param;
using Math::BoolVec4Ref;
using Math::BoolVec4Ptr;

using Math::IntVec2;
using Math::IntVec2Param;
using Math::IntVec2Ref;
using Math::IntVec2Ptr;

using Math::IntVec3;
using Math::IntVec3Param;
using Math::IntVec3Ref;
using Math::IntVec3Ptr;

using Math::IntVec4;
using Math::IntVec4Param;
using Math::IntVec4Ref;
using Math::IntVec4Ptr;

using Math::Vec2;
using Math::Vec2Param;
using Math::Vec2Ref;
using Math::Vec2Ptr;

using Math::Vec3;
using Math::Vec3Param;
using Math::Vec3Ref;
using Math::Vec3Ptr;

using Math::Vec4;
using Math::Vec4Param;
using Math::Vec4Ref;
using Math::Vec4Ptr;

using Math::Mat2;
using Math::Mat2Param;
using Math::Mat2Ref;
using Math::Mat2Ptr;

using Math::Mat3;
using Math::Mat3Param;
using Math::Mat3Ref;
using Math::Mat3Ptr;

using Math::Mat4;
using Math::Mat4Param;
using Math::Mat4Ref;
using Math::Mat4Ptr;

using Math::Quat;
using Math::QuatParam;
using Math::QuatRef;
using Math::QuatPtr;
